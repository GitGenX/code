#include<stdio.h>
#include<stdlib.h>

struct tree
{
    int data;
    struct node *left;
    struct node *right;
};

struct node* createnode(int x)
{
    struct node* temp = ()
}

struct node* newnode(int data)
{
    struct node* 
}